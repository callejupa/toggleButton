import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    container: {
        marginTop: 50,
        alignItems: 'center'
    },
    button: {
        borderRadius: 5,
        padding: 10
    },
    icon: {
        width: 180,
        height: 180,
        tintColor: '#f1f1f1',
    },
    liked: {
        tintColor: 'green'
    },
    text: {
        marginTop: 20
    }
})